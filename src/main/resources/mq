mq
幂等性：
    在编程中一个幂等操作的特点是其任意多次执行所产生的影响均与一次执行的影响相同。通俗的讲就一个数据，或者一个请求，
给你重复来多次，你得确保对应的数据是不会改变的，不能出错。

mq保证幂等性：
  由于网络延迟等问题，可能造成消息重复消费

  1、全局唯一 使用全局MessageID，解决幂等性。生产者生产消息的时候加上唯一的ID，数据库查询是否有此消息。
  2、利用redis的原子性去实现。